name: financecloud
description: "Control de gastos empresariales - FinanceCloud"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^1.1.0
  image_picker: ^1.2.0
  google_generative_ai: ^0.4.6
  permission_handler: ^11.3.1
  flutter_dotenv: ^6.0.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
    # - assets/images/otra_imagen.png
    # Incluye el archivo .env (para que esté accesible en build si lo deseas)
    - .env
